language: node_js

node_js:
  - "iojs"
  - "8"

cache:
  directories:
    - "node_modules"
    - "public"
git:
  depth: 3

branches:
  only:
  - dev
  - master

install: npm install

before_script:
  - npm install -g mocha

script:
  - npm start